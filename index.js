// # Review - Classes

// NOTE! Pay close attention to creating code with good and consistent code style.

// 1. Create a class called "Component".
// The constructor for Component should take in one parameter "name". 
//There should be one method called "render" that prints `<p>Component _name_  rendering` where `_name_`  is the provided name.


class Component{
    constructor(names){
        this.names=names;
    }
    render(){
        console.log()

